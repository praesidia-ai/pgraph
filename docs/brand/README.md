# PGraph by Praesidia

The connected lowercase **p** combines a graph with an enclosing counter: precise
repository knowledge and Praesidia's protective identity. One blue node marks the
context selected from the wider graph.

- Product name: **PGraph**.
- Brand attribution: **by Praesidia**.
- CLI and tool prefix: `pgraph` / `pgraph_`.
- Primary mark: [pgraph-mark.png](pgraph-mark.png), generated with the built-in
  image-generation tool and saved with its original alpha channel.
- Editor activity icon: [pgraph.svg](../../apps/vscode-extension/media/pgraph.svg),
  a simplified native SVG adaptation for small monochrome editor surfaces.
- Generation specification: [logo-prompt.md](logo-prompt.md).

Use the primary mark on a light neutral background so the dark enclosure remains
clear. Keep padding around the mark; use the theme-aware SVG inside editor chrome.
Write “PGraph by Praesidia” alongside the mark in documentation and distribution UI.
